#pragma once
#include <EntityArchetype.h>

/*
namespace ECS
{

	struct EntityQuery
	{
	public:
		EntityQuery(TID includeTypes[MAX_TYPE_IDENTIFIER_COUNT]) :
			m_includeTypes(includeTypes)
		{}
	private:
		TID m_includeTypes[MAX_TYPE_IDENTIFIER_COUNT];
	};

}
*/
